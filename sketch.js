function setup() {
  // Create a canvas that fills the entire browser window
  createCanvas(windowWidth, windowHeight);
}

function draw() {
//This is the background  
background(51,220,210);
circle(380,270,100);
}
